{
    "configurations": [

        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost/ramin/",
            "webRoot": "${workspaceFolder}"
        }

    ]
}
